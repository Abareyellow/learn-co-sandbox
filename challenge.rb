# 1.) Write a script that user input and passes it to a method as an argument and then displays it into the following concatenated string.

def speak(name) 
  puts "Hello #{name}, Nice to meet!"
end 
my_name = gets.chomp
speak(my_name)

# 2.) Correct the code below so it displays the sum of x, y and z.
array = {
  "x" 
}
def sum_numbers(x,y)
  x + y + z
end
sum_numbers